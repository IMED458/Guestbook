# არქიტექტურა

## რატომ ასე

სისტემა სამ სრულიად განსხვავებულ აუდიტორიას ემსახურება და თითოეული სხვა
უსაფრთხოების საზღვრის მიღმაა:

| | ვინ | რას ხედავს | როგორ შემოდის |
|---|---|---|---|
| **ADMIN** | SUPER_ADMIN, STAFF | მთელი ბიზნესი | username + პაროლი |
| **CLIENT** | კლიენტის ანგარიში | მხოლოდ საკუთარი `clientId` | username + პაროლი |
| **GUEST** | სტუმარი | ერთი საჯარო გვერდი | QR, ანგარიშის გარეშე |

## რატომ დაემატა სერვერი

ოთხი ოპერაცია ბრაუზერში უსაფრთხოდ **ვერ** შესრულდება:

1. **სხვა მომხმარებლის შექმნა** — Firebase client SDK ახლად შექმნილი
   მომხმარებლით *შენ* გასისტემებს, ანუ ადმინისტრატორს საკუთარი სესიიდან
   აგდებს.
2. **R2 ატვირთვის ხელმოწერა** — `R2_SECRET_ACCESS_KEY` bundle-ში მოხვედრა
   მთელ bucket-ს ხსნის.
3. **როლის მინიჭება** — თუ როლს ბრაუზერი ირჩევს, ის როლი არ არსებობს.
4. **ფაილის წაშლა storage-იდან** — იგივე secret.

ამიტომ **ერთი** Cloudflare Worker (`workers/api/`). არა ორი: Workers-ს
WebCrypto-თი შეუძლია service-account JWT-ის ხელმოწერა და Google OAuth
token-ის აღება, ამიტომ Firebase Functions მხოლოდ მეორე cold start-სა და
მეორე ბილინგს დაამატებდა, უსაფრთხოებას კი — არაფერს.

```
ბრაუზერი ──── Firestore (rules) ─────────────┐
    │                                         │
    │  ID token                               │ role/clientId/perms
    ↓                                         │  custom claims-იდან
Cloudflare Worker ── service account ─────────┘
    │
    ├── Identity Toolkit  (მომხმარებლების CRUD, claims)
    └── R2 presigned URL
             ↑
        ბრაუზერი პირდაპირ ატვირთავს ── R2
```

დიდი ვიდეო **არასოდეს** გადის Worker-ზე: ბრაუზერი ხელმოწერილ URL-ს იღებს და
პირდაპირ R2-ს ელაპარაკება.

## ნდობის ჯაჭვი

როლი და `clientId` **Firebase custom claims**-ია. მათ მხოლოდ Worker წერს
(service account-ით). Claims კრიპტოგრაფიულადაა ხელმოწერილი Google-ის მიერ,
ამიტომ:

- `firestore.rules` ენდობა `request.auth.token.role`-ს;
- Worker ენდობა იმავეს, ID token-ის ვერიფიკაციის შემდეგ;
- ბრაუზერს როლის გაყალბება **არ** შეუძლია.

STAFF-ის დეტალური უფლებები claims-ში ინდექსებადაა (`perms: [0,1,4]`), რადგან
custom claims 1000 ბაიტით შემოიფარგლება. ინდექსების რიგი ერთდროულად სამ
ადგილასაა კონტრაქტი:

- `src/domain/roles.ts` → `PERMISSIONS`
- `workers/api/src/auth.ts` → `PERMISSION_INDEX`
- `firestore.rules` → `ORDERS_VIEW()` და დანარჩენი

**ამ სიის გადალაგება აკრძალულია** — მხოლოდ ბოლოში დამატება. გადალაგება ჩუმად
გადაანაწილებს ყველა არსებულ უფლებას.

## უფლებები სამ დონეზე

| დონე | რას აკეთებს | ფაილი |
|---|---|---|
| UI | მალავს იმას, რაც არ გეკუთვნის | `src/lib/session.tsx`, `src/lib/routes.ts` |
| Firestore | კრძალავს მონაცემთან წვდომას | `firestore.rules` |
| Worker | კრძალავს პრივილეგირებულ ოპერაციას | `workers/api/src/auth.ts` |

ღილაკის დამალვა უსაფრთხოება არ არის — ამიტომ სამივე.

## ფული

თანხა **ყოველთვის** მთელი თეთრია: 500.00 ₾ = `50000`. `0.1 + 0.2` float-ებში
`0.30000000000000004`-ია; თეთრში ზუსტად `30`. ყველა გამოთვლა ერთ ადგილას —
`src/domain/orders.ts`. ორი კომპონენტი, რომლებიც ბალანსს ოდნავ სხვადასხვა
ფორმულით ითვლის, კლასიკური გზაა იმისა, რომ შეკვეთის გვერდი და ინვოისი
ერთმანეთს არ დაეთანხმონ.

## Media

| | სად |
|---|---|
| ორიგინალი (შეუხებელი) | R2 `clients/{id}/events/{id}/albums/{id}/originals/{uuid}-{name}` |
| Preview (gallery) | R2 `.../previews/{uuid}.jpg` — **ორიგინალს არ ცვლის** |
| Metadata | Firestore `albumMedia` |

ორიგინალზე არ ხდება resize, re-encode, JPEG კონვერტაცია და ტრანსკოდირება.
HEIC რჩება HEIC-ად.

Firestore-ში ბაიტები **არასოდეს** ინახება — მხოლოდ `objectKey` და metadata.

### Legacy

ძველი Cloudinary ჩანაწერები რჩება და ჩვეულებრივ იხსნება:
`storageProvider: 'cloudinary'` + აბსოლუტური `url`. ახალი ატვირთვები
ყოველთვის `storageProvider: 'r2'` + `objectKey`.

## Routing

Hash routing რჩება — GitHub Pages ერთ დოკუმენტს აბრუნებს, ამიტომ
`#/admin/orders/42` refresh-საც უძლებს და გაზიარებასაც, rewrite წესების და
`404.html`-ის ხრიკების გარეშე. მარშრუტების ცხრილი — `src/lib/routes.ts`,
სადაც თითოეული admin მარშრუტი თავის უფლებას ასახელებს, ასე რომ sidebar-ი და
guard-ი ერთი სიიდან იკითხება და ერთმანეთს არ ასცდება.
